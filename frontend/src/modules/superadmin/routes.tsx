/**
 * Super Admin route tree lives inside SuperAdminApp so teammates
 * can keep adding supervisor/telecaller routes in AppRouter without
 * merging this module's pages.
 */
export { default as SuperAdminApp } from './SuperAdminApp';
